name: desafio_teia
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.9.4+5
  csv: ^6.0.0
  path_provider: ^2.0.2 
  http: ^0.13.3
  uni_links: ^0.5.1
  image_picker: ^0.8.4+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 

flutter:
  uses-material-design: true
